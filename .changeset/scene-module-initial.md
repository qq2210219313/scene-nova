---
"scene-nova": minor
---

实现 Scene 模块：新增 `Scene` 类（继承自 `THREE.Scene`），提供三维环境的简化管理。

- 构造时可传入初始配置（背景、雾、环境贴图、覆盖材质、背景亮度 / 模糊度）
- 背景支持四种类型并可按类型传入配置：纯色、二维图片、立方体天空盒（`CubeTexture`）、等距柱状全景（HDR / 360°），纹理类背景支持 `intensity`（亮度）与 `blurriness`（模糊度）
- 环境贴图（含 URL 加载）、线性雾 / 指数距离雾、覆盖材质
- 所有设置方法支持链式调用
